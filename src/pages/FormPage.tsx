import { TableForm } from "../components";
import { FC } from "react";

const FormPage: FC = () => {
  return (
    <section className="h-full">
      <TableForm />
    </section>
  );
};

export default FormPage;
